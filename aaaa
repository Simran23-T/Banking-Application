# -- SQL SETUP ---
# -- Run this in your MySQL console or tool like phpMyAdmin

# CREATE DATABASE IF NOT EXISTS `Python_Force`;
# USE `Python_Force`;

# -- Drop tables if they exist (optional)
# DROP TABLE IF EXISTS transactions;
# DROP TABLE IF EXISTS bank_accounts;

# -- Table to store bank accounts
# CREATE TABLE bank_accounts (
#     ID INT  PRIMARY KEY,
#     owner_name VARCHAR(255),
#     phone VARCHAR(15) UNIQUE NOT NULL,
#     account_number BIGINT NOT NULL UNIQUE,
#     balance FLOAT NOT NULL DEFAULT 0.0,
#     total_credited FLOAT DEFAULT 0.0,
#     total_withdrawn FLOAT DEFAULT 0.0,
#     pin VARCHAR(10) NOT NULL,
#     password VARCHAR(255) NOT NULL
# );

# -- Table to log all transactions
# CREATE TABLE transactions (
#     ID INT  PRIMARY KEY,
#     account_number BIGINT NOT NULL,
#     transaction_type VARCHAR(50) NOT NULL,
#     amount FLOAT NOT NULL,
#     transaction_time DATETIME NOT NULL,
#     FOREIGN KEY (account_number) REFERENCES bank_accounts(account_number) ON DELETE CASCADE
# );
